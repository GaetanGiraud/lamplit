# Prettier writes LF; keep the repository LF everywhere so a Windows checkout
# does not show every file as modified.
* text=auto eol=lf
*.ico binary

# Screenshots in docs/ are generated; keep git out of them.
*.png binary
